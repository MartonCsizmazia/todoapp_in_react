import React from 'react';

export default function SearchBox() {
    return (
        <input
            type="text" autoFocus
            className="form-control search"
            placeholder="Search"
        />
    );
}
