import React from 'react';

export default function Search() {
    return (
        <input
            type="text" autoFocus
            className="form-control search"
            placeholder="Search"
        />
    );
}
